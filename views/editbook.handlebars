<h1>Edit your book</h1>

<form action="/books/updatebook" method="POST" class="form">
    <input type="hidden" name="id" value="{{book.id}}"> <!-- passa o id pelo POST sem aparecer ao usuário-->
    <div class="form-control">
        <label for="title"> Title: </label>
        <input type="text" name="title" placeholder="Write you book title" value="{{book.title}}">
    </div>

    <div class="form-control">
        <label for="author"> Author: </label>
        <input type="text" name="author" placeholder="Write author name" value="{{book.author}}">
    </div>

    <div class="form-control">
        <label for="pageqty"> Pages: </label>
        <input type="number" name="pageqty" placeholder="Number of pages" value="{{book.pageqty}}">
    </div>

    <input type="submit" value="Edit"> 

</form>